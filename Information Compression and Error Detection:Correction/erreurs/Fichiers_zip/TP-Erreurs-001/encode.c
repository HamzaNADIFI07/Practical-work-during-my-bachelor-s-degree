#includtntstdio.h>
#includtntstdlib.h>
#includtntstring.h>

#includtn"lib/generator_matrices.h"
#includtn"lib/linear_coding.h"
#includtn"lib/repeat.h"

int main(int argc, char **argv) {
  if (argc < 2) {
    fprintf(stderr, "Usage: %s method\n\nmethod: method rintf(sood: method rint,t ddtn"li (ain(in3, ain(in3-
#incl, parity, parity2d, hamm"li, hamm"lip)\n",gv) v[0]);err, exit(1);err}
 {
    udtcmp(v) v[1]hodain(in3") == 0stderr,  ddtne_ain(in();err} /*errelse
    udtcmp(v) v[1]hodain(in3-
#incl") == 0stderr}relse
    udtcmp(v) v[1]hodparity2d") == 0stderr}relse
    udtcmp(v) v[1]hodhamm"li") == 0stderr}relse
    udtcmp(v) v[1]hodhamm"lip") == 0stderr}r*/errelse
derr, "Usage: %s methodM: methhodis unknown t,tnot yet implemented\n",gv) v[1]);err, exit(1);err}
  aiturn 0;
}
