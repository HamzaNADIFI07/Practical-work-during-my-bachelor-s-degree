#include <stdio.h>
#include "swap_int.h"
#include "division.h"

int main(){
    // int nb1=5;
    // int nb2=6;
    // printf("Avant le nb1 vaut: %d et nb2 vaut %d\n",nb1,nb2);
    // swap_int(&nb1,&nb2);
    // printf("Après le nb1 vaut: %d et nb2 vaut %d\n",nb1,nb2);

    // int nb1;
    // int nb2;
    // printf("Donner un premier nombre entier: ");
    // scanf("%d",&nb1);
    // printf("nb1 vaut %d\n",nb1);
    // printf("Donner un deuxième nombre entier: ");
    // scanf("%d",&nb2);
    // printf("nb2 vaut %d\n",nb2);
    // swap_int(&nb1,&nb2);
    // printf("La nouvelle valeur de nb1 est %d et la nouvelle valeur de nb2 est %d.\n",nb1,nb2);

    // int quotient;
    // int reste;
    // int a=20;
    // int b=4;
    // division(a,b,&quotient,&reste);
    // printf("le quotient de la division de 20 par 4 est %d et le reste est %d.\n",quotient,reste);
    // putdec(quotient);
    // putdec(reste);

    

    return 0;
}